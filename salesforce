getAllUserFromApex() {
      getRemovedGroups({
         batchrecordId: this.recordId
      }).then(result => {
         console.log('User == ', JSON.stringify(result))
         this.deletedOptions = result
      }).catch(error => {
         this.deletedOptions = undefined;
      });
   }
